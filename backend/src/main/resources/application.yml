spring:
  application:
    name: backend

  datasource:
    url: jdbc:mysql://localhost:3306/erms_db
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true

  jackson:
    time-zone: Asia/Kolkata

server:
  port: 8080